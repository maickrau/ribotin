#ifndef VerkkoReadAssignment_h
#define VerkkoReadAssignment_h

#include <vector>
#include <string>

std::vector<std::vector<std::string>> getReadNamesPerCluster(std::string verkkoBaseFolder, const std::vector<std::vector<std::string>>& nodesPerCluster);

#endif
